image: eu.gcr.io/ligatus-prod-public-registry/generic-build-agent:latest

stages:
  - build

build:
  stage: build
  script:
  - mvn clean package -Dlicense.skip=true -DskipTests -Pbuild-shaded-jar
  
  artifacts:
    paths:
      - pinot-distribution/target/pinot-*-pkg.zip
